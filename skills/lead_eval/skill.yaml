camel: 1.0.0
name: le/lead_eval
title:  lead_eval Skill
description: Update this description --  lead_eval
inputs:
  - name: input1
    title: input1
    parameters:
      - name: text
        type: string
        description: Update this description
        required: true
    routing:
      all:
        action  : le/lead_eval
        runtime: cortex/functions
        output: output1

outputs:
  - name: output1
    title: output1
    parameters:
      - name: text
        type: string
        description: Update this description
        required: true
